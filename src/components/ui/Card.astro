---
interface Props {
  class?: string;
}
const { class: className = '' } = Astro.props;
---

<div class={`min-w-0 overflow-hidden rounded-xl border border-zinc-700/80 bg-zinc-900/70 p-5 shadow-sm ${className}`}>
  <slot />
</div>
